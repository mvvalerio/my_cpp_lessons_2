#include <iostream>

using namespace std;


auto name = "Paulo";            // vai auto declarar a variavel, é usado para quando não sabemos o que vai ser exatamente
auto num = 123;                 // tem que estar inicializada (ter valor)
auto pi = 3.14;

unsigned int altura = 10;       // não pode ser negativo, assim sendo só pode ser positivo e 0

int main(){
    system("clear");

    cout << name << "\a \n";

    return 0;
}